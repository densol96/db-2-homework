export type ALL_TABLES = {
  tableNames: string[];
  tablesTotal: number;
  totalResults: number;
};

export type SINGLE_TABLE = {
  result: object[];
  pagesTotal: number;
  totalResults: number;
};
